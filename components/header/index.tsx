import { usePathname } from "expo-router";
import { StyledView } from "../styleds/components";

export default function Header() {
    const pathname = usePathname()
    return (
        <StyledView>
        </StyledView>
    )
}